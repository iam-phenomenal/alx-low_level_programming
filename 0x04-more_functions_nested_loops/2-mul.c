#include "main.h"

/**
 * mul- Entry
 * @a: first parameter
 * @b: second parameter
 * Description: Compute the multiplication of a and b
 * Return: computed value
*/

int mul(int a, int b)
{
	return (a * b);
}
