#include "main.h"

/**
 * main- Entry
 * Description: Print lowercase alphabets
 * Return: 0(Success)
*/

int main(void)
{
	char start = 'a';
	for (start; start <= z; start++)
	{
		_putchar(start);
	}
	_putchar('\n');
	return (0);
}
